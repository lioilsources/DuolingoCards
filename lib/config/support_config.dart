/// Where user feedback lands. No backend — reports travel as plain e-mail
/// composed on the user's device, so this address is the entire "server".
const String kSupportEmail = 'oldrich.vorechovsky.jr@gmail.com';
